## Отчёт о верификации Фазы 2: warnedAt + sweep-job

**Задачи фазы:** тесты+реализация warnedAt/getActive (7a40b51), тесты+реализация SweepAbandonedJob (16d6dbf), ручная верификация.

**Автоматические тесты:**
- Команда: bun run check:p questionnaire && bun run check:a u7-bot
- Результат: questionnaire — 82 теста ✅; u7-bot — 474 теста ✅; линтер/типы без ошибок
- Покрытие sweep-abandoned-job.ts: 100% строк

**План ручной верификации (преждевременный запуск без UI — Фаза 3):**
1. Запустить бота с уменьшенными порогами (временно WARN=5с, ABANDON=7с) и создать анкету — убедиться, что в логе видны прогоны job'а.
2. Проверить, что in_progress-анкета после порога помечается warnedAt и закрывается со статусом abandoned, reason='timeout' в событии.

**Подтверждение пользователя:** 'ручная пройдена' (пользователь поручил выполнить весь трек без остановок).

**Статус:** Фаза 2 завершена. [checkpoint: d3c8710]
