# Спецификация экранов Courses (UI Spec)

Документ описывает экраны Telegram-бота модуля Courses: их содержимое, кнопки, условия видимости и доменную логику.
Служит **отправной точкой для разработки** — что уже реализовано, что запланировано.

Условные обозначения:
- ✅ реализовано
- ❌ не реализовано
- 📋 в бэклоге / запланировано в треке

---

## S00 — Каталог курсов: 5-уровневый drill-down (✅ реализовано)

**Как попасть:** «📖 Программы курсов» в главном меню.
**Кому:** всем ролям.
**Формат:** как в «Моя учёба → Уроки» — каждый уровень показывает текущие объекты жирным + кнопками, подуровень — inline текстом.

### Уровни:

| Уровень | Заголовок | Жирный+кнопка | Inline (текст) | Кнопка «Назад» |
|---------|-----------|---------------|----------------|----------------|
| 0 | `📖 *Курсы*` | Курсы | Этапы (с числом модулей) | `↩️ Главное меню` |
| 1 | `📖 *Курс: {title}*` | Этапы | Модули (с проектами/уроками) | `⬅️ Назад к курсам` |
| 2 | `📖 *Этап: {title}*` | Модули | Проекты (с числом уроков) | `⬅️ Назад к курсу` |
| 3 | `📖 *Модуль: {title}*` | Проекты | Уроки (с числом шагов) | `⬅️ Назад к этапу` |
| 4 | `📖 *Проект: {title}*` | Уроки | Шаги (заголовки, без тел, макс. 3) | `⬅️ Назад к модулю` |

### Пример S00 (уровень 0):
```
📖 *Курсы*
💻 *Курс: Основы программирования*
    🗂️ Этап: Синтаксис — 1 модуль
    🗂️ Этап: Алгоритмика — 1 модуль

💼 *Курс: Бизнес-аналитика*
    🗂️ Этап: Анализ — 1 модуль
```

### Примечания:
- Карточки курса S00a нет — клик на курс сразу ведёт на уровень этапов.
- Кнопка «📚 Найти поток» убрана (кросс-контроллерная навигация — техдолг).

---

## Желание пройти курс (запись на курс) — ✅

Модуль `wish` фиксирует **желание пользователя пройти курс**. UI этих экранов живёт в контроллере курсов: у модуля `wish` **нет своего bot-ui-контроллера** — он вызывается через `appApi` из стори каталога курсов.

### Путь пользователя

```
Каталог курсов (S00, уровень 0) — карточка курса
  └─ кнопка «🎓 Хочу пройти курс» → UC wish:create-course-wish
       ├─ курс БЕЗ пула анкеты → желание expressed → W03
       └─ курс С пулом анкеты → желание pending + анкета (questionnaire S01–S06)
              ├─ ✅ questionnaire:complete → ER wish:confirm-wish → confirmed → W03
              └─ ✅ questionnaire:abandon → ER wish:abandon-wish → abandoned
```

### W01 — Кнопка «🎓 Хочу пройти курс» (✅)

**Где:** карточка курса на уровне 0 каталога (S00) — вторая кнопка рядом с «{курс}».

| Текст | Код | Действие | Статус |
|-------|-----|----------|--------|
| `🎓 Хочу пройти курс` | `course:course-catalog:apply:{courseId}` | → UC `create-course-wish` | ✅ |
| `➡️ Следующий модуль` / `🔁 Пройти модуль снова` | `course:course-catalog:wish:{moduleId}` | → UC `create-module-wish` (кнопка из уведомления о завершении, трек wish-module) | ✅ |

**Условия видимости (решение трека D):**
- кнопка рендерится **всегда**; при активном желании (`expressed | pending | confirmed`) или обучении клик ведёт на W04 — конфликт обрабатывается экраном, скрытия кнопки нет;

### W02 — Анкета желания (только если у курса есть пул анкеты) (✅)

Делегировано модулю `questionnaire` (экраны S01–S06 в `questionnaire/ui-spec.md`).

- UC `create-course-wish` **сразу** создаёт желание в статусе `pending` и лишь затем запускает анкету (`ownerInfo = { courseId }`);
- пул анкеты задаётся в `packages/wish/src/domain/wish/pools/course.json` (map `courseId → QuestionnairePool`);
- `questionnaire:complete` → ER `confirm-wish` → `confirmed`;
- `questionnaire:abandon` → ER `abandon-wish` → `abandoned`;
- если у курса **нет** пула — шаг пропускается, желание фиксируется сразу (W03).

### W03 — Желание зафиксировано (✅)

**Как попасть:** мгновенно (курс без пула → `expressed`) или после завершения анкеты (`confirmed`).

**Что происходит:** UC `create-course-wish` создал `Wish(user, target=course)` в `pending`/`expressed`; статус доведён до `expressed`/`confirmed`.

**Содержание:**

```
🎯 Твоё желание пройти курс зафиксировано!
```

| Текст | Код |
|-------|-----|
| `↩️ Главное меню` | `app:main-menu` |
| `📝 Мои заявки` | 📋 бэклог |

### W04 — Желание уже есть / анкета начата (✅)

**Как попасть:** повторный клик «Хочу пройти курс» при активном желании (`expressed | pending | confirmed`) или обучении — UC `create-course-wish` возвращает `WISH_ALREADY_EXISTS` (payload содержит `status`).

**Содержание — ветвление по статусу:**

`pending` (анкета начата, не завершена):

```
📝 Ты начал заполнять анкету по этому курсу, но не закончил её.
Продолжи — и желание будет закреплено.
```

| Текст | Код |
|-------|-----|
| `▶️ Продолжить анкету` | `questionnaire:fill:resume:{courseId}` (через `Routes`) |
| `↩️ Главное меню` | `app:main-menu` |

`expressed | confirmed` (желание уже выражено) / обучение:

```
📝 Ты уже выразил желание пройти этот курс.
```

или

```
📚 Ты уже обучаешься на этом курсе.
```

| Текст | Код |
|-------|-----|
| `🗑️ Отменить желание` | `course:course-catalog:cancel:{courseId}` (→ W05) |
| `↩️ Главное меню` | `app:main-menu` |

### W05 — Отмена желания (✅)

**Как попасть:** кнопка «🗑️ Отменить желание» на W04 (ветки `expressed | confirmed`); для `pending` отмены нет — анкетная ветка закрывается через `questionnaire:abandon`.

Шаг 1 — подтверждение (`cancel:{courseId}`):

```
Отменить желание пройти курс?
```

| Текст | Код |
|-------|-----|
| `✅ Да` | `course:course-catalog:cancel-confirm:{courseId}` → UC `cancel-wish` |
| `❌ Отмена` | `course:course-catalog:phases:{courseId}` (возврат к карточке) |

Шаг 2 — после подтверждения:

```
🗑️ Желание пройти курс отменено.
```

| Текст | Код |
|-------|-----|
| `↩️ Главное меню` | `app:main-menu` |

Гонка (желание уже отменили): `WISH_NOT_FOUND` → мягкое сообщение «ℹ️ Активного желания на этот курс уже нет." без ⚠️.

---

## Доменные замечания (для треков)

- Пока только реализация «Создание желания».
- Роль `CANDIDATE` **удаляется** — информация о желании живёт в `Wish`.
- Цель желания — `WishTarget` (дискриминированный союз, сейчас только `course`); агрегат `Wish` не знает про курсы.
- Статусы `Wish` (полный жизненный цикл):
  - мгновенная ветка: `expressed` → `cancelled | fulfilled`;
  - анкетная ветка: `pending` → `confirmed | abandoned`, далее `confirmed` → `cancelled | fulfilled`;
  - отмена (`cancel-wish`) доступна только из `expressed | confirmed`; для `pending` — только через `abandon`.
- Скоуп активности: не более одного активного желания на пару (user, target); параллельные желания на разные курсы разрешены; повторное создание после `cancelled`/`abandoned` разрешено.
- **Предложение о реализации → трек wish-invite:** при создании потока (набор открыт) всем желающим курса отправляется приглашение с кнопками «Открыть поток» и «Отменить желание».
- **Реализация желания (трек C2):** при зачислении пользователя в поток — `Wish` помечается `fulfilled` (переход из `expressed | confirmed`, через ER на событие зачисления).
- **Отмена желания:** в приглашении (трек wish-invite) есть кнопка отмены желания.
- Курс без пула анкеты — желание фиксируется **мгновенно** по клику (без questionnaire).
- Брошенная анкета (`pending` без активности): 6 часов простоя — предупреждение с кнопкой продолжения, 8 часов — автоматический abandon (трек job-scheduler).

---

## Бэклог

- 📋 «Мои заявки» — список желаний пользователя со статусами и отменой.
- 📋 Другие типы целей желания (расширение `WishTarget`: mentorship, challenge, …).
