# Onboarding Controller — UI Spec (заглушка)

**Статус:** 📋 отложено до Релиза 4 (metrics)

Контроллер перенесён из `packages/onboarding/src/ui/bot/` без изменений логики.
Кнопка «📝 Заполнить анкету» отключена — не возвращается из `handleStart()`.

Полноценный рефакторинг на стори будет выполнен в рамках трека metrics (Релиз 4).
