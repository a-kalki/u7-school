Отчёт о верификации Фазы 1: Улучшение сообщений об ошибках валидации

Автоматические тесты:
- Команда: cd packages/core && bun test
- Результат: 64 pass, 0 fail, 185 expect() calls (17 файлов)

Шаги ручной верификации:
1. Запуск всех тестов пакета @u7/core — 64 теста проходят
2. Проверка ключевых изменений:
   - format-valibot-errors.ts — форматирование Valibot issues (5 тестов)
   - console-controller.ts — интеграция с formatValibotErrors (3 теста, включая новый)
   - use-case.ts — сохранение rawIssues в payload ошибки валидации
   - auto-ui-module.ts — проброс ошибок до контроллера (6 тестов)

Подтверждение пользователя: да (04.05.2026)
