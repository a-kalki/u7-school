Задача: Перевести стори (fill, invite, render.ts) на контракт «Диалог и Экран»

Сводка: render.ts — renderActionResponse возвращает DialogResponse (wait_next → screen edit-in-place; stale → notify warn-реплика; new_question → finalize+screen; completed → finalize+screen+release; invited → S01-экран); respondInContext-хелперы (editOrSend, renderPreviousQuestion) удалены; подсказка текстового ввода S03 «Введите ваш ответ текстом...» добавлена. FillStory — handleCallback/handleMessage/handleCommand на BotSession: qId из dialog.input.context; /cancel активной → stop с confirm-экраном S05a (abandon только по fill:cancel-confirm); fill:cancel:{qId} — confirm из S07/S09; fill:current:{qId} — восстановление флоу с awaitInput; resume → awaitInput по фильтру standard+in_progress+courseId; валидация — errorNotify (ввод живёт). InviteStory — start:q → delegate fill:current:{qId} (ввод адресуется fill-диалогу); подписка questionnaire:invite → ProactiveSender.invite с ПОЛНЫМИ кодами (Routes.questionnaire.inviteStart/Why/Decline — новые фабрики) + подсказка /start. Подписки fill (start/warning/continue-invite) — вариант A: invite-канал, кнопка-мост resume, «Прервать» → fill:cancel:{qId} (confirm); abandon timeout → notify; by_user — без дубля. Routes.questionnaire.fillCancel — новая фабрика.

Тесты: inventory.test.ts + fill.dialog.test.ts (26) + invite.dialog.test.ts (8) + warning (6) + continue-invite (3) = 61, все зелёные. Перенос сценариев (протокол «тесты не исчезают»): fill.story.test.ts (4 подписки→fill.dialog; start-вариант A→fill.dialog; resume-фильтр→fill.dialog; completionText/fallback→fill.dialog; подсказка /cancel→fill.dialog; шапка прогресса→fill.dialog) — удалён; radio.test.ts (маркеры \(x\)/клавиатуры→inventory+fill.dialog finalize) — удалён; multiple.test.ts (тогглы/Далее→inventory S02b+fill.dialog wait_next/finalize; «Далее» при пустом драфте→fill.dialog) — удалён; stale.test.ts (warn-реплики/logger/empty_selection→fill.dialog; перерисовка-сценарии устарели по контракту) — удалён; invite.story.test.ts (S01-кнопки→inventory; markdown-безопасность→invite.dialog; start→delegate) — удалён; warning/continue-invite — переписаны на invite-канал с полными кодами.

Файлы: fill.story.ts, invite.story.ts, render.ts (переписаны), routes.ts (+фабрики), 5 тест-файлов удалены, 2 переписаны, 3 новых.

Верификации: bun test questionnaire 61/61 ✓; полный бот 432 pass, 49 fail — все mentor (трек 5, вне скоупа); biome скоупа чист; tsc unit-скоупа 0 ошибок (e2e questionnaire-ux/wish-questionnaire — фаза 3).

Причина: Green-фаза задач 1-2; решения владельца: прерывание только после подтверждения (2026-09-10), S03-подсказка добавлена, вариант A для проактивов.
