Контрольная точка Фазы 1: Мгновенный переход к следующему шагу

Автоматические тесты:
- Команда: bun run check:p stream
- Результат: 145 pass, 0 fail (unit)
- Интеграционные: 11 pass, 0 fail (learning.integration.test.ts)
- E2E: 16 pass, 0 fail (user-flows.e2e.test.ts)
- Lint: чисто
- TSC: без ошибок

Ручная верификация:
1. Запустить бота (bun run dev)
2. Как студент: «Моя учёба» → «Выполнено» → следующий шаг (без пром. сообщения)
3. Конец урока → поздравление + «Начать следующий урок»
4. Конец проекта → поздравление + «Начать следующий проект»
5. Конец потока → «Поток полностью завершён!»

Подтверждение пользователя: да
