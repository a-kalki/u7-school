# Шаги урока: Функция forEach(arr, callback) — перебор массива

---

### Создание ветки для проекта 7

**kind:** `text`

Начинаем новый проект «Итеративные методы». Создай новую ветку от `main`:

```bash
git switch main
git pull
git switch -c feat/iterative-methods
```

Все функции этого проекта, как и в проектах 5 и 6, живут в папке `arrays/`. Убедись, что она существует: `ls arrays/`.

> Если предыдущая ветка ещё не смерджена в `main`, создавай ветку от неё, чтобы иметь доступ к её функциям (например, `len`).

---

### Реализация forEach()

**kind:** `text`

> Убедись, что ты в ветке `feat/iterative-methods`.

Реализуй функцию `forEach(arr, callback)` — перебор массива с вызовом callback для каждого элемента. Создай файлы `arrays/for-each.js` и `arrays/for-each.test.js`, следуй TDD.

**Функция принимает:**
- `arr`: `array` — массив для перебора
- `callback`: `function` — функция, которая вызывается для каждого элемента

**Функция возвращает:** `undefined`.

**Важно:** функция **иммутабельная** — не меняет исходный массив. Callback получает три аргумента: `(элемент, индекс, массив)`.

Тестовые случаи (каждый — отдельный `test(...)`):
- Должна вызвать callback для каждого элемента: для `[1, 2, 3]` проверить, что callback вызван 3 раза с правильными значениями
- Должна не вызывать callback для пустого массива: `forEach([], fn)` — `fn` не вызывается ни разу (можно положить в нее `throw Error('not be called'))`.
- Должна вернуть `undefined`: `forEach([1], () => {})` → `undefined`
- Должна выбросить `TypeError` если `arr` не массив
- Должна выбросить `TypeError` если `callback` не функция

**Подсказка по тестированию callback-вызовов:**
```js
test('должна вызвать callback для каждого элемента', () => {
  const calls = [];
  forEach([1, 2, 3], (item, i, _arr) => {
    calls.push({ el, i, arr });
  });
  expect(calls).toEqual([
    { item: 1, i: 0, arr: [1, 2, 3] },
    { item: 2, i: 1, arr: [1, 2, 3] },
    { item: 3, i: 2, arr: [1, 2, 3] },
  ]);
});
```

---

### Сохрани изменения

**kind:** `text`

Когда все тесты пройдут:

```bash
git add arrays/for-each.js arrays/for-each.test.js
git commit -m "Реализована forEach — перебор массива с callback"
```
