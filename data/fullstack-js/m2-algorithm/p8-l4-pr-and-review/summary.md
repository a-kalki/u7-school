# Финальный прогон и PR для проекта 8 — краткая выжимка

- Запустить `bun test` — все тесты (включая старые функции) должны быть зелёными
- Проверить JSDoc на всех 4 функциях: параметры, возврат, throws, особенности
- Проверить `git status` — clean, `git log --oneline` — осмысленная история
- **Ветка:** `feat/iterative-methods-2`
- Пуш: `git push -u origin feat/iterative-methods-2`
- PR: заголовок «Проект 8: Массивы — find, reduce, flat», перечислить все функции с особенностями
- Отметь: `find`/`findIndex` — ранний выход, `reduce` — два режима, `flat` — рекурсия + reduce
- 2 ревьюера + pin-сообщение в чате

[Полный конспект](./lesson.md)
