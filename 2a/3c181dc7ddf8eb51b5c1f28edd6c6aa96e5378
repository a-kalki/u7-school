Warn-телеметрия мёртвых кнопок (решение владельца)

1. Кнопка без обработчика → warn-лог (code/dialogPath/actor) в двух точках: контроллер (ни одна стори не узнала префикс) и стори (нет ветки). TelegramLogger пропускает WARN со всех источников — доходит админу, фильтр не меняли.
2. Кнопку «Уведомить о наборе» удалить в треке 2 — задача внесена в план nav (Фаза 2, streams).
3. Grep-инвариант 'каждый cbFor имеет ветку' — отклонён владельцем (избыточно).
4. Инцидент верификации: Фаза 3 была закоммичена с 3 красными тестами (test.each, ожидал 'Объект' во всех кейсах) — вывод bun обрезался tail -2, fail-строка не была видна. Исправлено; урок: проверять строку fail явно.
