Задача: ФР-3 — валидация кнопок без исключений (Фаза 1, Task 4)

Сводка: порядок валидации callback зафиксирован док-комментарием и тестами: закрытый диалог → alert «Наберите /start»; штамп ≠ dialog.seq → «Экран устарел — нажмите /start»; shortId из прошлой жизни → alert про рестарт. Тест штампа ~0 адаптирован: при закрытом диалоге сообщение «Наберите /start» (было «Экран устарел»).

Файлы: apps/u7-bot/src/infra/bot-transport.ts (doc), apps/u7-bot/src/infra/bot-transport.test.ts (адаптация теста).

Причина: механика guard'ов реализована в ФР-1/ФР-2, здесь — фиксация контракта сообщениями и тестами.
