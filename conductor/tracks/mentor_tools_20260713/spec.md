# Спецификация трека: Инструменты ментора

> **Контекст эволюции:** `conductor/architecture-evolution.md` (§2.3, §2.8, §2.10, §2.11). Прочитать перед началом.
> **Зависимости:** трек 0 (Course), трек 1 (ЖЦ студента), трек 2 (ContentPath), трек 5 (curious_showcase_20260708 — прозрачность S07/S08).

## Обзор

Объединённый трек: всё, что нужно ментору для управления потоками и мониторинга студентов.

**Часть A — Управление потоками:** выделить управление в отдельное role-gated пространство «🛠️ Инструменты ментора». Сейчас кнопка «➕ Создать поток» висит в корне главного меню, а lifecycle-кнопки смешаны с curious-кнопками. Цель — навести порядок: curious-ветка «Потоки курсов» (трек `curious_showcase_20260708`) — только обзорная, без менторских инструментов.

**Часть Б — Мониторинг студентов:** автоматическое выявление отстающих, сводка здоровья группы, сортировка проблемных сверху, детальная история шагов студента со статистикой времени.

**Роли:** MENTOR, ADMIN — управление потоками. S07/S08 контент — всем ролям (прозрачность из трека 5). Кнопки действий над студентом — только ментор потока / ADMIN.

**UX-принцип:** всё управление студентами — из карточки потока. Нет отдельных точек входа «Мониторинг». Ментор заходит в поток → видит студентов → действует.

---

## Часть А: Управление потоками

### F1. Главное меню «🛠️ Инструменты ментора»
- Подменю (только для MENTOR/ADMIN):
  - «📋 Мои потоки» — потоки, где пользователь является ментором → **S02m mentor-режим**.
  - «➕ Создать поток» — перенос `CreateStreamStory.handleStart` с корневого уровня на второй.
- Кнопки «👥 Мониторинг» **нет** — мониторинг встроен в карточку потока (кнопка «👥 Студенты» в S02m).

### F2. S02m — mentor-режим карточки потока
- Содержимое карточки:
  - Название, ментор, статус, описание, даты
  - `👥 Студенты` → **S07** — список студентов потока с кнопками-строками
  - `📊 Сводка потока`: счёт по статусам (active/advanced/not_advanced/abandoned/enrolled). Медиана прогресса. Число отстающих/критических.
  - **Внизу** — панель управления lifecycle:
    - `🚀 Запустить` (enrollment → active)
    - `✅ Завершить` (active → completed)
    - `📁 В архив` — **только для завершённых (COMPLETED) потоков**.

**Переходы:**
- Нажатие на студента (кнопка-строка в S07) → **S08** (история шагов + кнопки управления).
- В S08 ментор видит историю шагов и кнопки действий: `⚠️ Неактивен`, `✅ Завершить`, `🔄 Сменить исход`.

### F3. Curious-ветка — БЕЗ lifecycle-кнопок (✅ уже сделано)
- В треке `curious_showcase_20260708` lifecycle-кнопки убраны из `ViewStreamStory.#buildKeyboard`.

### F4. Перенос «Создать поток» на второй уровень
- `CreateStreamStory.handleStart` → `null` (не участвует в главном меню). Вход — через подменю.

---

## 🏗️ Реализация S02m — варианты и контекст

**Что уже готово (трек `curious_showcase_20260708`):**
- `ViewStreamStory` — curious-карточка: просмотр, программа, детали, студенты. Без lifecycle-кнопок.
- `ViewStreamMentorStory` — менторские хендлеры: `complete`, `archive`, `complete-confirm`, `archive-confirm` + confirm-диалоги. Лежит в `packages/stream/src/ui/bot/stories/view-stream-mentor.story.ts`, тесты в `describe.skip`.
- `ActivateStreamStory` — уже зарегистрирован в `StreamController`, работает.
- Кнопки S02m описаны в `ui-spec.md`: Запустить/Завершить/В архив + Студенты с действиями.

**Вариант А: Зарегистрировать ОБА сторис в MentorController**
- `MentorController` регистрирует `ViewStreamStory` (curious) + `ViewStreamMentorStory` (lifecycle).
- Ментор видит curious-карточку (свою) и lifecycle-кнопки (mentor-сторис).
- Плюс: без дублирования кода, `ViewStreamStory` используется как есть.
- Минус: две разные сторис в одном контроллере → клавиатура собирается из двух мест.

**Вариант Б: S02m = дочерний сторис с собственной клавиатурой**
- `ViewStreamMentorStory` не только обрабатывает lifecycle, но и строит полную S02m-клавиатуру (curious + lifecycle).
- Либо наследует и переопределяет `#buildKeyboard` (но `#`-приватные методы в JS не наследуются — потребуется рефакторинг `#` → `protected`).
- Либо дублирует `#handleView` в менторском сторис. Плюс: одна точка входа, одна клавиатура.
- Минус: дублирование или рефакторинг приватных методов.

**Рекомендация:** Вариант А проще в реализации, вариант Б чище архитектурно (одна клавиатура). Решение — на усмотрение разработчика трека.

---

## Часть Б: Мониторинг студентов

> **Важно:** действия ментора в S08 (`mark-abandoned`, `complete-student`) уже реализованы в треке 1. Эта часть **не создаёт их заново** — добавляет bot-level тесты и перерабатывает **контент** S07/S08.

### F5. Логика «Отстаёт» (S07, S08)

**Два уровня отставания:**

| Уровень | Условие | Маркер |
|---------|---------|--------|
| 🛑 **Критическое** | >7 дней с момента `completedAt` последнего шага, статус `active` | 🛑 «Стоп» |
| ⚠️ **Отстаёт** | >4 дней с момента `completedAt` последнего шага, статус `active` | ⚠️ |
| ⚠️ **Отстаёт** (альтернативное) | Прогресс ниже медианы группы на 30%+ | ⚠️ |
| ✅ **Норма** | Ни одно из условий выше не выполнено | ✅ |

**Правила:**
- Если выполняются оба условия (и по дням, и по медиане) — приоритет по дням (🛑/⚠️).
- Маркер виден **всем** ролям в S07 (прозрачность).
- В S08 — текстовое объяснение причины («Не заходил 8 дней», «Прогресс 25% при медиане группы 70%»).
- Ментор может отчислить через кнопку `⚠️ Неактивен` (cause=`inactivity`).

### F6. Сводка по группе (S07)
- Заголовок: «👥 Студенты потока» + «Всего: N»
- Статистика: медиана прогресса, число отстающих (⚠️), критических (🛑), advanced, not_advanced, active, enrolled.
- Видна всем ролям (прозрачность здоровья группы).

### F7. Список студентов (S07)
- **Каждый студент — отдельная кнопка-строка:**
  ```
  🛑 Анна Петрова — 25% (3/12)
  🏃 Иван Смирнов — 40% (5/12)
  🏃 Олег Иванов — 70% (9/12)
  ✅ Мария Ким — Завершила (advanced)
  ```
- **Сортировка:**
  1. 🛑 Критические — в самом верху
  2. ⚠️ Отстающие
  3. 🏃 В процессе — по убыванию прогресса
  4. ✅ Завершённые (advanced/not_advanced/abandoned) — в конце
- **Каждая строка студента — ряд из 2-3 кнопок:**
  - `{Маркер} {Имя} — {Прогресс}` → **S08** (карточка со статистикой)
  - `🛑 Неактивен` — `mark-abandoned` (только для active)
  - `✅ Завершить` / `🔄 Сменить исход` — в зависимости от статуса

  Пример:
  ```
  🛑 Анна Петрова — 25% (3/12)      [⚠️ Неактивен] [✅ Завершить]
  🏃 Иван Смирнов — 40% (5/12)      [⚠️ Неактивен] [✅ Завершить]
  🏃 Олег Иванов — 70% (9/12)       [⚠️ Неактивен] [✅ Завершить]
  ✅ Мария Ким — Завершила (adv)     [🔄 Сменить исход]
  ```

  Ментор может действовать **сразу из списка**, не заходя в карточку студента.

### F8. Карточка студента (S08)
- Открывается по нажатию на имя студента в S07.
- **Содержание:** имя, статус, прогресс, причина отставания (если есть).
- **Статистика по времени выполнения шагов** (без списка шагов):
  - Для каждого завершённого шага вычисляется `completedAt - issuedAt`.
  - Шаги группируются по категориям, показывается **только количество**:

  | Категория | Время | Отображение |
  |-----------|-------|-------------|
  | 🏃 Листатель | < 1 мин | «🏃 Листатель: 3» |
  | ⚡ Быстро | 1–5 мин | «⚡ Быстро: 5» |
  | 📝 Нормально | 5–15 мин | «📝 Нормально: 12» |
  | 📚 Углублённо | > 15 мин | «📚 Углублённо: 4» |

  Позволяет ментору одним взглядом оценить паттерн: «листатель» (🏃 доминирует), осмысленно (📝/📚), смешанно.

- **Кнопки управления** — **только в S07**, в S08 не дублируются.
- Видна всем ролям (прозрачность).

### F9. Действия ментора в S08 — bot-level тесты
Действия уже в коде (трек 1). Требуются bot-level интеграционные тесты через `BotRouter.handleCallback`:
- `✅ Завершить` → выбор исхода → confirm → UC → статус изменился, `−STUDENT`
- `⚠️ Неактивен` → `mark-abandoned` (cause: `inactivity`) → end-to-end
- `🔄 Сменить исход` для завершённого → смена outcome

---

## Критерии приёмки
- [ ] «🛠️ Инструменты ментора» → подменю (Мои потоки / Создать поток), role-gated.
- [ ] «Мои потоки» → S02m с lifecycle-кнопками.
- [ ] «➕ Создать поток» из подменю, не в корне меню.
- [ ] В curious-режиме lifecycle-кнопок нет.
- [ ] Маркеры студентов: 🛑 (>7 дней), ⚠️ (>4 дней или <30% медианы), 🏃 (в процессе), ✅ (завершён).
- [ ] Студенты в S07 — кнопки-строки с кнопками действий (Неактивен / Завершить / Сменить исход).
- [ ] S07 показывает сводку здоровья группы (всем), сортировка: 🛑 → ⚠️ → 🏃 по прогрессу → ✅.
- [ ] Карточка студента (S08) содержит статистику времени (🏃/⚡/📝/📚) и причину отставания, без кнопок действий.
- [ ] `complete-student` UI работает end-to-end через BotRouter.
- [ ] Bot-level тесты действий ментора в `tests/bot`.
- [ ] Покрытие >80%, TDD.

## За рамками
- «Инструменты автора» (AUTHOR) — отложено, только в документации (§2.8).
- Broadcast/рассылка группе (бэклог).
- Автоматическое отчисление по неактивности (только ручное `mark-abandoned`).
- Графики/аналитика во времени.
