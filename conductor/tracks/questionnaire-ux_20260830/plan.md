# План реализации — Трек: UX анкет (questionnaire-ux_20260830)

## Фаза 1: Мультивыбор — редактирование на месте

- [ ] Task: Написать падающие unit-тесты FillStory: тоггл варианта → `editMessage` того же сообщения (`messageId` из `session.lastBotMessage`), маркеры `[x]`/`[ ]` обновлены, клавиатура сохранена
- [ ] Task: Написать падающие unit-тесты: кнопка «Далее» рендерится только при ≥1 выбранном варианте; при снятии всех вариантов — исчезает
- [ ] Task: Реализовать в `#renderActionResponse`/`#callUc` обработку `wait_next` как editMessage (паттерн `editOrSend` из `controllers/learning/shared.ts`); fallback на sendMessage, если `lastBotMessage` нет (проактивный старт/resume)
- [ ] Task: Реализовать «Далее»: edit текущего сообщения с финальными маркерами и **без клавиатуры** + sendMessage следующего вопроса (комбинированный BotCommand)
- [ ] Task: Обновить существующие тесты `fill.story.test.ts` под новое поведение
- [ ] Conductor - Ручная верификация 'Мультивыбор — редактирование на месте'

## Фаза 2: Одиночный выбор — маркировка и автопереход

- [ ] Task: Написать падающие unit-тесты: выбор radio → `editMessage` с маркером `(x)` у выбранного варианта и **без клавиатуры** + `sendMessage` следующего вопроса
- [ ] Task: Реализовать radio-ветку рендеринга: комбинированная команда editMessage + sendMessage; автопереход без кнопки «Далее»
- [ ] Task: Тест: история «вопрос → выбранный ответ» сохраняется (предыдущее сообщение остаётся в чате с маркировкой, без кнопок)
- [ ] Conductor - Ручная верификация 'Одиночный выбор — маркировка и автопереход'

## Фаза 3: Регрессии, e2e, документация

- [ ] Task: Проверить проактивные сценарии: `questionnaire:start`/`invite`, resume, abandon-warning, `/cancel` — unit-тесты актуальны, поведение не сломано
- [ ] Task: Проверить likert-анкеты на совместимость с новым рендерингом; адаптировать при необходимости
- [ ] Task: Написать интеграционные/e2e тесты полного сценария заполнения анкеты (radio + multiple + text) в u7-bot по `conductor/code_styleguides/bot-test.md`
- [ ] Task: `bun run check:a u7-bot`
- [ ] Conductor - Ручная верификация 'Регрессии, e2e, документация'

## Финал

- [ ] Task: `bun run check` (все пакеты)
- [ ] Task: Сводка финальной ручной верификации трека
