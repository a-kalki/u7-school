# План реализации — Трек: UX анкет (questionnaire-ux_20260830)

## Фаза 1: Мультивыбор — редактирование на месте

- [x] Task: Написать падающие unit-тесты FillStory: тоггл варианта → `editMessage` того же сообщения (`messageId` из `session.lastBotMessage`), маркеры `[x]`/`[ ]` обновлены, клавиатура сохранена (7115fed)
- [x] Task: Написать падающие unit-тесты: кнопка «Далее» рендерится только при ≥1 выбранном варианте; при снятии всех вариантов — исчезает (7115fed)
- [x] Task: Реализовать в `#renderActionResponse`/`#callUc` обработку `wait_next` как editMessage (паттерн `editOrSend` из `controllers/learning/shared.ts`); fallback на sendMessage, если `lastBotMessage` нет (проактивный старт/resume) (7115fed)
- [x] Task: Реализовать «Далее»: edit текущего сообщения с финальными маркерами и **без клавиатуры** + sendMessage следующего вопроса (комбинированный BotCommand) (7115fed)
- [x] Task: Обновить существующие тесты `fill.story.test.ts` под новое поведение (не потребовалось: тесты без lastBotMessage идут по fallback; 7115fed)
- [ ] Conductor - Ручная верификация 'Мультивыбор — редактирование на месте'

## Фаза 2: Одиночный выбор — маркировка и автопереход

- [x] Task: Написать падающие unit-тесты: выбор radio → `editMessage` с маркером `(x)` у выбранного варианта и **без клавиатуры** + `sendMessage` следующего вопроса (03faa97)
- [x] Task: Реализовать radio-ветку рендеринга: комбинированная команда editMessage + sendMessage; автопереход без кнопки «Далее» (покрыта общим new_question-рендером Фазы 1; 03faa97)
- [x] Task: Тест: история «вопрос → выбранный ответ» сохраняется (предыдущее сообщение остаётся в чате с маркировкой, без кнопок) (03faa97)
- [ ] Conductor - Ручная верификация 'Одиночный выбор — маркировка и автопереход'

## Фаза 3: Takeover — явный перехват ввода (core + анкета)

- [x] Task: Расширить `KeyboardDescription` опциональным полем `takeover?: boolean` (packages/core) (95c789d)
- [x] Task: Написать падающие тесты uiApp: кодирование маркера в `code` при отправке (интерактивный ответ + проактивный send) и снятие при приёме (95c789d)
- [ ] Task: Написать падающие тесты uiApp: при активном чужом `activeHandler` + takeover-флаг — callback НЕ блокируется, захват перезаписывается; без флага — прежняя блокировка (95c789d)
- [x] Task: Реализовать маркер-префикс и логику обхода блокировки в `packages/core/src/ui/bot/ui-app.ts` (95c789d)
- [x] Task: Написать падающие тесты транспорта: при `takeover === true` кнопки и `session.activeHandler != null` вниз текста добавляется «⚠️ Нажатие на кнопку приведёт к окончанию вашего текущего действия.»; без активного действия строка не добавляется (95c789d)
- [x] Task: Реализовать добавление строки в `BotTransport.execute()` (по структурному полю, не по маркеру) (95c789d)
- [x] Task: Кнопка «▶️ Продолжить анкету» (resume) в FillStory получает `takeover: true` + тесты (95c789d)
- [ ] Conductor - Ручная верификация 'Takeover — явный перехват ввода'

## Фаза 4: Job продолжения брошенных анкет (3/6/9)

- [x] Task: Написать падающие тесты агрегата: поле `continueInvitedAt`, отметка без сброса `updatedAt` (обход safeUpdate, как `markWarned`) (13c85ff)
- [x] Task: Реализовать `continueInvitedAt` в entity + метод отметки в a-root (13c85ff)
- [x] Task: Написать падающие тесты job'а: ступени 3ч/6ч/9ч, каждая — один раз; интервал 3ч; сброс цепочки при возобновлении (13c85ff)
- [x] Task: Константы в файле job'а: `INVITE_AFTER_HOURS = 3`, `WARN_AFTER_HOURS = 6`, `ABANDON_AFTER_HOURS = 9`, `INTERVAL_MS` (13c85ff)
- [x] Task: Реализовать эволюцию `SweepAbandonedJob`: интервал 3ч, новая ступень приглашения, закрытие 8ч→9ч; событие приглашения (кнопка с takeover) (13c85ff)
- [x] Task: FillStory: обработка события приглашения — сообщение + кнопки «▶️ Продолжить анкету» (takeover) / «⏭️ Прервать» + тесты (13c85ff)
- [ ] Conductor - Ручная верификация 'Job продолжения брошенных анкет'

## Фаза 5: Регрессии, e2e, документация

- [ ] Task: Проверить проактивные сценарии: `questionnaire:start`/`invite`, resume, предупреждения (6ч/9ч), `/cancel` — unit-тесты актуальны, поведение не сломано
- [ ] Task: Проверить likert-анкеты на совместимость с новым рендерингом; адаптировать при необходимости
- [ ] Task: Написать интеграционные/e2e тесты полного сценария заполнения анкеты (radio + multiple + text) в u7-bot по `conductor/code_styleguides/bot-test.md`
- [ ] Task: Написать e2e тест takeover-сценария: активный урок → проактивное приглашение с takeover-кнопкой → перехват ввода → заполнение анкеты
- [ ] Task: `bun run check:a u7-bot`
- [ ] Conductor - Ручная верификация 'Регрессии, e2e, документация'

## Финал

- [ ] Task: `bun run check` (все пакеты)
- [ ] Task: Сводка финальной ручной верификации трека
