Задача Фазы 7: зафиксировать техдолг планировщика (TODO.md, секция «Планировщик Job»).

1. JobExecutor → воркер: тяжёлые job'ы — вынести прогон в отдельный процесс через порт JobExecutor; контракт Job не меняется. Предусловие — внешнее хранилище (in-memory/JSON-репозитории недоступны из другого процесса).
2. Симметричный жизненный цикл init/start/stop по слоям: сейчас start/stop есть только у ApiApp/UiApp, модули имеют init(); единый контракт — кандидат в отдельный трек.

Проверки: bun run check — exit 0.
