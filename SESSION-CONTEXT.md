# Контекст сессии — трек bot-ui-dialog-lifecycle (1.1), сброс 2026-09-08 (вечер)

> Временный файл для восстановления контекста после сброса сессии.
> Удалить, когда фаза 3 закрыта.

## Что делали (завершено)

Трек «Жизненный цикл диалога» (`conductor/tracks/bot-ui-dialog-lifecycle_20260905/`):

- **Фаза 1** (вход в диалог, seq, валидация кнопок) — done, верифицирована.
- **Фаза 2** (единый вход команд: конверт, транспорт без grammy-регистрации) — done, верифицирована.
- **Фаза 2.1** (ревизия ФР-4: трёхуровневый pipe, ядро без имён команд, welcome/help — U7BotUiApp, контракт isActive, /start — uiApp) — done, верифицирована.
- **Фаза 2.2** — done, верифицирована владельцем 2026-09-08 (checkpoint-коммит + notes):
  - активная стори первой в pipe; контракт ввода «стори обязана ответить»; транспорт без next;
  - `invite` (ФР-6): без диалога — «временно активный» якорь `app/invite` (seq=1; единственное место, где транспорт пишет session.dialog в обход enterDialog), приглашение не умирает;
  - дефолт `BotUiStory.handleMessage` (не abstract): warn разработчику (awaitInput без обработчика — программная ошибка; исключение не бросаем — контроллер превратил бы его в «внутреннюю ошибку») + реплика-отказ «Извините, на данном этапе сообщения не принимаются»;
  - `/help` — второй прямой путь uiApp (мимо pipe): активной стори задаётся публичный `contextHelp()`, всем остальным (меню, закрытый диалог, активная без справки) — общий справочник; неактивная стори на /help не отвечает никогда (активность проверяет uiApp). `/cancel` остался в pipe (условная логика isActive);
  - концепция: `/log_level` — НЕ исключение (обычный pipe, обработчик — app-контроллер); прямые пути — только /start и /help.

## Что нужно сделать (следующая сессия)

**Фаза 3 (kind-уведомления)** — задачи в плане готовы к выполнению:

1. Падающие тесты рендера: проактив `kind` (🔔/ℹ️/⚠️, дефолт 'notify'), диалоговая реплика `notify` с kind (дефолт 'info'), `errorNotify` — переспрос ввода без захвата экрана.
2. ФР-5: `NoticeKind = 'notify' | 'info' | 'warn'`; `NotificationPayload.kind` (замена `tone`); `DialogResponse.notify = { text, kind }` — переименование слота `info` → `notify` + поглощение `Screen`; хелпер `errorNotify(err)` у стори; единая таблица рендера в транспорте; обновить потребителей ядра.
3. Ручная верификация 'Kind-уведомления' по протоколу workflow.md.

Дальше — Фаза 4 (документация: bot-ui-session-architecture.md §4–6/§10, lifecycle.md; обновление треков 2–5; summary; финальные проверки).

## Опорные файлы

- Концепция: `conductor/guides/bot-ui-concept.md` (новый, читать первым).
- Спека/план трека: `conductor/tracks/bot-ui-dialog-lifecycle_20260905/{spec,plan}.md`.
- Ядро: `packages/core/src/ui/bot/{types,ui-app,bot-controller,bot-ui-story}.ts`.
- Транспорт: `apps/u7-bot/src/infra/bot-transport.ts` (реплики ввода, invite-якорь, штампы).
- u7-контракт: `apps/u7-bot/src/core/{ui-app,u7-bot-ui-story,u7-bot-controller,u7-menu}.ts` (ui-app: #commandHelp/#activeStory).

## Состояние проверок

- `bun run check:p core` — зелёный (279 тестов).
- u7-бот скоуп (core/, controllers/app/, infra/) — зелёный (125 тестов); biome чисто; tsl: в src/core и src/infra ошибок нет.
- Легаси-стори (streams/courses/mentor/questionnaire/learning, e2e-тесты) — заявленно красные, миграция в треках 2–5.
- tsl-wiring: 5 заявленных легаси-ошибок create-ui-app (легаси-контроллеры, как до фазы).
