Отчёт о верификации Фазы 2: Упреждающие подсказки при вводе команд (итоговый)

Автоматические тесты:
- Команда: cd packages/core && bun test
- Результат: 66 pass, 0 fail, 205 expect() calls

Изменения фазы:
- renderUseCasePrompt: блок кода сохранён + секция **Параметры:** с типами, enum-ами, опциональностью
- describeField: анализ Valibot-схем (тип, picklist/enum, optional/nullable, array)
- isArraySchema: определение полей-массивов
- executeUseCase: для массивов — разбивка строки по запятой (одна строка = одно поле)
- Подсказка для массивов: «через запятую, напр. `- значение1, значение2`»

Подтверждение пользователя: да (04.05.2026)
