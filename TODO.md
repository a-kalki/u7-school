# Todo задачи

**Архитектурные:**
1. Нужен базовый класс `WizardStory` с унифицированным движком пошагового ввода: контекст, переходы, обработка ошибок валидации. Сейчас логика wizard дублируется в `CreateStreamStory`.

**UI бота (stream):**
1. `MonitorStory`: кнопка «📁 История шагов» в карточке студента не реализована — возвращает «⚠️ Неизвестная команда». Нужно реализовать обработчик `history:*` с показом списка шагов студента и их статусов.

**Инструменты ментора:**
1. в карточке "Студенты" (S07), ментору идут подсказки:
  - ⚠️ человек который уже не занимался в течении трех дней подряд;
1. в карточке "Студент" (S08) ментор может "Отчислить" студента (можно подобрать другой термин). Через подтверждение.
1. Ментор может каждому активному студенту выдать статус что он успешно закончил данный модуль.
1. Ментор может завершить поток. Через подтверждение. Для каждого активного студента присваивается статус что он окончил модуль, но не проходит дальшеk
1. завершение потока должно снять у всех студентов потока роль "Студент";

**Инструменты студента:**
1. Кнопку первого уровня "Моя учеба" приводит к кнопкам:
  - "Список уроков": можно получить список уроков по модулям. Выводится подсказка и ожидание ввода шаблона;
    - mA — выдаст все проекты и уроки модуля (доступно для всех);
    - mA:pB — выдаст все уроки определенного модуля и проекта (доступно для всех);
    - mA:pB:lC — выдаст все наименования уроков и шагов (доступно для всех);
    - mA:pB:lC:all — выдаст контент всех шагов урока (доступны только пройденные);
    - mA:pB:lC:sD — выдаст контент шага (доступно только пройденные);
  - предусмотреть возможность реализации предыдущего пункта через кнопки, насколько это будет лучше по ux?
  - "Текущий урок": получить текущий шаг урока;

**Инструменты любопытного**
1. "Наши потоки" -> <Поток> -> "Программа" приводит к выдаче списка проектов, уроков по данному модулю

**Доменные:**
