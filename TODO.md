# Todo задачи

**Архитектурные:**
1. `ContentSnapshot` дублирован между `course/domain/types.ts` (чистый TS-интерфейс) и `stream/domain/stream/entity.ts` (valibot-схема). Нужно устранить дублирование: course — единственный source of truth для типа, stream — использует тип из course. Импорты `ContentSnapshot` в stream-сториз разнятся (часть из course, часть из stream).

**Доменные:**
1. `studentId` в cb-data кнопки «Выполнено» избыточен — студент однозначно определяется через `actor.uuid`. Нужно убрать `studentId` из `complete:<studentId>:<streamId>:<stepId>`, оставив `complete:<streamId>:<stepId>`. Заодно в `#handleComplete` сначала получать студента по `actor.uuid`, сверять `streamId`, потом вызывать `complete-step`.
1. Нужно добавить показ кнопки "Сообщество школы", которая ведет в группу школы (ранее было что то вроде команды /join...);
1. При создании потока (stream), хорошо бы вставлять имена, описания, цели и прочее с курса как значение по умолчанию.
1. Как студент через "Моя учеба" может получить текущий шаг. Если нажатие на "Моя учеба" сразу выдает ее, то нажатие на выполнено должно выдать сразу следующий шаг с "Выполнено"+"Мой прогресс". Сообщение "Шаг выполнен! ..." не нужно, это и так понятно. Но если был закончен урок (проект), то можно выдать поздравление и кнопку "Начать следующий урок".
1. Нужно написать bot-tests (integration, e2e) для модуля onboarding.
1. Хорошо бы всегда когда заходим в иерархию меню, всегда предусматривать возврать назад спец кнопкой. Для Второго уровня (после главного меню с команды /start) можно кнопку "/start".
1. хорошо бы сделать что то вроде help для верхнего меню. Все контроллеры переопределяющие handleStart как правило определяют handleHelpStart возвращающее строку (или объект): "Наши потоки - показывает список потоков курсов, как текущих, так и завершенных"
1. Хорошо бы в сообщение "Потоки школы" добавить легенду, что означают каждый из цветных кружков эмодзи.
