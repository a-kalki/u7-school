{
	"name": "@u7/course",
	"version": "0.1.0",
	"module": "src/index.ts",
	"type": "module",
	"private": true,
	"scripts": {
		"test": "bun test",
		"lint": "bunx biome check .",
		"tslint": "tsc --noEmit",
		"format": "bunx biome check . --write",
		"check": "bun run lint && bun run tslint && bun test"
	},
	"dependencies": {
		"@u7/user": "workspace:*",
		"valibot": "^1.0.0"
	}
}
