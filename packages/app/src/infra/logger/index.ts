/**
 * Инфраструктурные утилиты логирования уровня приложения.
 *
 * CompositeLogger позволяет объединять несколько логгеров
 * (например, ConsoleLogger + TelegramLogger) в один.
 */

export { CompositeLogger } from './composite-logger';
