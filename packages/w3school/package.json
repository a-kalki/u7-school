{
  "name": "@u7/w3school",
  "version": "0.1.0",
  "module": "src/index.ts",
  "bin": {
    "w3s": "./src/cli/index.ts"
  },
  "type": "module",
  "private": true,
  "scripts": {
    "test": "bun test",
    "lint": "bunx biome check .",
    "tslint": "tsc --noEmit",
    "format": "bunx biome check . --write",
    "check": "bun run lint && bun run tslint && bun test"
  },
  "dependencies": {
    "cheerio": "^1.2.0",
    "turndown": "^7.2.4"
  }
}
