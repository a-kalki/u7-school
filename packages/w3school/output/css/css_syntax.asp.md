# CSS Syntax

* * *

## CSS Syntax

A CSS rule consists of a selector and a declaration block:

![CSS selector](img_selector.gif)

The selector points to the HTML element you want to style.

The declaration block contains one or more declarations separated by semicolons.

Each declaration includes a CSS property name and a value, separated by a colon.

Multiple CSS declarations are separated with semicolons, and declaration blocks are surrounded by curly braces.

```javascript
p {  color: red;  text-align: center;}
```

#### Example Explained

*   `p` is a selector in CSS (it points to the HTML element you want to style: <p>).
*   `[color](https://www.w3schools.com/cssref/pr_text_color.php)` is a property, and `red` is the property value
*   `[text-align](https://www.w3schools.com/cssref/pr_text_text-align.php)` is a property, and `center` is the property value

You will learn much more about CSS selectors and CSS properties in the next chapters!

* * *

* * *

## Video: CSS Syntax

  [![Tutorial on YouTube](images/yt_logo_rgb_dark.png)

 ![Tutorial on YouTube](images/css_syntax.png)](https://youtu.be/G8r00ZNopTE&list=PLP9IO4UYNF0UCaUSF3XNZ1U9f01E5h5PM)

* * *

* * *