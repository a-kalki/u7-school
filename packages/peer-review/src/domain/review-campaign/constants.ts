/**
 * Длительность окна сбора отзывов кампании `stream_completed` (дней).
 *
 * Константа домена пакета (не UI и не конфиг UC): фабрика вычисляет
 * `expiresAt` при создании кампании и сохраняет его в состоянии —
 * истёкшость дальше читается агрегатом, а не пересчитывается на лету.
 */
export const REVIEW_WINDOW_DAYS = 7;
