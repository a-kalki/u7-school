Контрольная точка Фазы 1 (Прокидка ProactiveSender).

Автотесты: bun test packages/core/src/ui/bot/ → 67 pass, 0 fail; tsc --noEmit → чисто.

Ручная верификация (для пользователя):
1. Убедиться, что проект собирается и запускается: bun run check:a u7-bot.
2. Проверить, что при старте бот корректно инициализирует UiApp (uiApp.init(resolve, transport) + subscribeEvents).
3. Кнопки проактивных сообщений анкеты после прокидки получают полный префикс questionnaire:... (проверяется после Фазы 2, т.к. рендеринг переносится в FillStory).

Подтверждение: выполнено в автономном режиме по запросу пользователя «сделай весь трек полностью».
