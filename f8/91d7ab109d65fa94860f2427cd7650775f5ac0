Фаза 5 трека core-api-actor_20260914. Приложения мигрированы: u7-bot (U7ApiApp, botAdminUser — резолв BOT_ADMIN при старте в main.ts + createUiApp стал async), u7-cli (currentActor — полный User через get-user), scripts (resolveActor через публичный get-user). Полный гейт CI=true bun run check: 2092 теста / 0 fail, biome+tsc чисты. Попутно: biome-format conductor/code_styleguides/troubleshoots/registry.json (чужой файл, чистое форматирование — требовалось для гейта; content не менялся).
