Отчёт верификации — Фаза 2.2 «Правки ревью» (трек bot-ui-dialog-lifecycle_20260905)

Дата: 2026-09-08

Автоматические проверки (команды):
- bun run check:p core → 279 pass / 0 fail; biome чисто; tsc --noEmit — без ошибок в src/core
- bun test apps/u7-bot/src/{core,infra,controllers/app} → 125 pass / 0 fail; biome чисто; tsc --noEmit — без ошибок в src/{core,infra}

Ручная верификация: подтверждена владельцем 2026-09-08 («считай что ручная проверка пройдена»).

Содержание фазы (коммит 95145060):
1. invite (ФР-6): без диалога транспорт открывает временный якорь app/invite (seq=1) —
   приглашение не умирает; кнопки живы до смены диалога (нажатие → switch в целевую стори,
   /start → reopen). Единственное место записи session.dialog в обход enterDialog
   (компромисс временного метода, задокументирован).
2. BotUiStory.handleMessage — не abstract: warn разработчику (awaitInput без обработчика)
   + реплика-отказ «Извините, на данном этапе сообщения не принимаются».
3. /help — второй прямой путь uiApp (мимо pipe): активной стори задаётся публичный
   contextHelp(), остальным — общий справочник; неактивная стори на /help не отвечает никогда.
4. Концепция bot-ui-concept.md: /log_level — не исключение (обычный pipe); судьба /help и /cancel.
5. ФР-6 в spec.md обновлён (якорь app/invite вместо текста с подсказкой /start).

Обновления документации: bot-ui-concept.md (§2 адресация, §5 инструкция), spec.md (u7UiApp,
контракт u7-стори), plan.md (задача правок ревью 2.2).
